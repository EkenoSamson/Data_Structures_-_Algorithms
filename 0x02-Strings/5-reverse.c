#include "strings.h"

/**
 * reverse - reverse a string.
 * @str: takes in string.
 * Return: the reversed string.
 */

char *reverse(char *str)
{
	/* get the lenght of the string*/
	while (*str != "\0")
	{
		count++;
		str++;
	}

}

